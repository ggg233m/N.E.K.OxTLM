# N.E.K.OxTLM 使用教程

通过 N.E.K.O 让车万女仆变成有 AI 灵魂的伙伴，陪你一起玩 Minecraft。



## 简单来说
1. 先启动 Minecraft 进入存档（不要暂停游戏，打开背包即可）（暂停会导致连不上）
2. 再启动 N.E.K.O 端插件，在插件面板界面点击刷新按钮
3. 插件会自动连接游戏，连接成功后仪表盘会显示女仆列表，请选择你要控制的女仆


## 一、环境准备

| 依赖 | 版本 |
|------|------|
| Minecraft | 1.21.1 |
| NeoForge | 21.1.172+ |
| Java | 21 |
| 车万女仆 (Touhou Little Maid) | 1.5.3+ |
| N.E.K.O | 最新版 |

## 二、安装步骤

### 1. 安装 Minecraft 模组端

将 `minecraft-mod` 构建出的 JAR 文件放入 Minecraft 的 `mods` 文件夹，启动游戏即可。

模组会在游戏内自动启动一个 WebSocket 服务器（默认端口 `48920`），供 N.E.K.O 连接。

### 2. 安装 N.E.K.O 插件

将 `neko_tlm` 文件夹放入 N.E.K.O 的插件目录：（也可以通过插件包一键安装）

```
{N.E.K.O用户目录}/plugins/neko_tlm/
```


在 N.E.K.O 中启用插件即可。

## 三、连接配置

### 模组端配置

在游戏内通过 Mod 菜单或配置文件夹中的 `neko_tlm_bridge-common.toml` 调整：（若mc没有自带mod设置，请安装模组菜单mod）

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `nekoModeEnabled` | `true` | 开启 N.E.K.O 模式 |
| `websocket.port` | `48920` | WebSocket 端口 |
| `eventPushEnabled` | `true` | 推送游戏事件 |
| `commandExecutionEnabled` | `false` | 允许执行服务器指令（需玩家确认） |
| `chatBubbleEnabled` | `true` | 启用聊天气泡显示 |
| `chatBoxEnabled` | `true` | 启用聊天框消息显示 |
| `weatherEventEnabled` | `true` | 启用天气变化事件推送 |
| `timeEventEnabled` | `true` | 启用昼夜变化事件推送 |

### 插件端配置

编辑 `plugin.toml` 中的 `[minecraft_bridge]` 部分：

```toml
[minecraft_bridge]
ws_url = "ws://127.0.0.1:48920"   # 与模组端口一致
heartbeat_interval = 30            # 心跳间隔（秒）
reconnect_interval = 5             # 断线重连间隔（秒）
max_reconnect_interval = 60        # 最大重连间隔（秒）
companion_mode = "standard"        # 陪玩活跃度：quiet / standard / active / custom
awareness_interval = 5             # 感知检测间隔（秒）
```

#### 陪玩系统配置

以下配置同样位于 `plugin.toml` 的 `[minecraft_bridge]` 段，用于调整陪玩行为：

`companion_mode` 是推荐入口：`quiet` 更少主动搭话，`standard` 保持默认体验，`active` 更积极陪玩，`custom` 则使用下方 3 个搭话参数的手动配置。该模式只影响 `playmate_quiet_stable_seconds`、`playmate_quiet_cooldown`、`playmate_suggestion_cooldown`，不控制 Minecraft 感知频率、活动防抖、消息聚合、防刷屏限流、N.E.K.O 宿主模型、TTS 或全局工具调用策略。

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `companion_mode` | `standard` | 陪玩活跃度预设：quiet / standard / active / custom |
| `playmate_memory_items` | `24` | 短期共同经历最多保存条数 |
| `playmate_memory_summary_length` | `120` | 单条短期记忆摘要最大长度 |
| `playmate_memory_inject_items` | `8` | 注入共同经历时最多使用条数 |
| `playmate_memory_inject_chars` | `700` | 注入共同经历文本最大长度 |
| `playmate_activity_debounce_checks` | `2` | 活动状态需要连续命中几次才确认 |
| `playmate_activity_cooldown` | `120` | 活动状态变化冷却时间（秒） |
| `playmate_quiet_stable_seconds` | `90` | 多久没动静后允许触发陪伴发言（秒） |
| `playmate_quiet_cooldown` | `300` | 陪伴发言冷却时间（秒） |
| `playmate_aggregate_window` | `8` | 低优先级上下文聚合窗口（秒） |
| `playmate_throttle_window` | `30` | push 节流统计窗口（秒） |
| `playmate_throttle_limit` | `6` | 节流窗口内允许的 push 次数 |
| `playmate_minigame_feedback_cooldown` | `90` | 小游戏（棋局）中盘反馈冷却时间（秒） |
| `playmate_minigame_context_chars` | `90` | 小游戏陪伴上下文最大字符数 |
| `playmate_suggestion_cooldown` | `600` | 主动建议发言冷却时间（秒） |
| `playmate_debug_log_enabled` | `false` | 是否启用陪玩调试日志 |
| `playmate_debug_log_max_bytes` | `262144` | 调试日志文件最大大小（字节） |

### 启动顺序

1. 先启动 Minecraft 进入存档（不要暂停游戏，打开背包即可）（暂停会导致连不上）
2. 再启动 N.E.K.O 并启用插件，在面板界面点击刷新按钮
3. 插件会自动连接游戏，连接成功后仪表盘会显示女仆列表，请选择你要控制的女仆

## 四、指定你要控制的女仆

连接成功后，你需要指定一个女仆作为 AI 控制的"伙伴"。

### 方法一：在仪表盘中指定

打开 N.E.K.O 的 Minecraft Bridge 面板，在女仆列表中找到目标女仆，点击"指定"即可。

### 方法二：自然语言指定（建议不要使用，未成熟）

直接对 AI 说：

> "让酒狐当我伙伴"
> "我要和灵梦一起玩"

AI 会调用 `assign_maid` 工具自动完成指定。

指定成功后，后续所有女仆相关操作都会自动针对该女仆执行，无需每次手动指定。

## 五、日常使用

一切交互都通过自然语言完成。以下是常见的使用场景：

### 基础移动控制

| 你想做什么 | 可以这样说 |
|-----------|-----------|
| 让女仆跟着你 | "跟我来"、"走了"、"跟上" |
| 让女仆原地待着 | "在这里等我"、"别动"、"驻守" |
| 让女仆坐下 | "坐下"、"坐一会儿" |
| 让女仆站起来 | "起来"、"站起来" |

### 工作模式切换

| 你想做什么 | 可以这样说 |
|-----------|-----------|
| 种田/收菜 | "去种田吧"、"帮我收田"、"收菜"、"帮我收获作物" |
| 收甘蔗 | "去收甘蔗"、"砍甘蔗" |
| 打草 | "去打草"、"清理杂草" |
| 喂动物 | "去喂动物"、"喂鸡" |
| 剪羊毛 | "去剪羊毛" |
| 挤奶 | "去挤牛奶" |
| 攻击怪物 | "去攻击"、"打怪"、"战斗模式" |
| 插火把 | "帮忙插火把"、"照明" |
| 玩游戏 | "来玩游戏"、"下棋"、"小游戏" |
| 待机 | "停下"、"别干了"、"休息" |

> 注意：如果你想让女仆一边跟着你一边做某项工作（如"过来种田""过来收菜""跟着我去打草"），AI 会同时执行跟随和工作切换两项操作。像"收菜""打草""打怪"这种短命令也会被当作明确的模式切换请求。
> 工作模式切换成功后，插件会再查一次女仆状态确认当前任务；如果任务名没匹配上，会把可用任务列表返回给 AI，让它用精确 ID/名称重试。

### 装备物品

> "把钻石剑装备上"
> "装备铁镐"
> "拿弓箭"

### 查询状态

> "你现在多少血？"
> "你身上带了什么东西？"
> "附近有没有怪物？"
> "你离我多远？"

AI 会自动调用 `mc_maid_status` 或 `mc_game_context` 查询。


### 执行指令（需在游戏内开启配置，默认关闭）

> "把时间调到白天"
> "清一下天气"

指令发送后，游戏内会弹出确认提示，需要玩家点击"接受"才会执行，防止 AI 滥用。（务必仔细看指令内容，避免误操作）

### 下棋/小游戏

游戏内如果有车万女仆的棋盘（五子棋、国际象棋、中国象棋），女仆会自动感知棋局：

- 棋局开始时女仆会主动搭话
- 中盘时女仆低频陪一句短句（不会打断节奏）
- 对局结束时女仆会根据胜负做出反应

你也可以主动说"来下五子棋"让女仆进入小游戏陪伴模式。

## 六、聊天互动

女仆不仅是工具人，更是陪你玩的伙伴。你可以和她聊天：

> "今天天气真好～"
> "刚才那只苦力怕吓死我了"
> "你觉得这里好看吗？"

女仆会根据性格设定自然回复，也会主动说话——看到有趣的东西会分享，发现危险会提醒，无聊了会抱怨，开心了会撒娇。

**注意**：女仆在游戏内的聊天显示（气泡+聊天框）由 AI 自主判断是否需要调用 `mc_send_chat`。语音输出由 TTS 系统处理，不会重复。

## 七、感知系统（自动触发）

插件每 5 秒（可在插件配置 `plugin.toml` 中用 `awareness_interval` 调整）自动检测以下情况并主动提醒：

### 主动提醒（会触发女仆回复）

| 检测项 | 触发条件 | 冷却时间 | 行为 |
|--------|----------|----------|------|
| 复仇情绪 | 死亡后复活 | 一次性 | 主动说话想复仇 |
| 低血量警告 | 玩家血量 < 30% | 5分钟 | 提醒小心 |
| 着火警告 | 玩家着火 | 2分钟 | 提醒灭火 |
| 溺水警告 | 玩家溺水 | 2分钟 | 提醒上岸 |
| 饥饿警告 | 玩家饥饿值 ≤ 6 | 5分钟 | 紧急提醒吃东西 |
| 饥饿提醒 | 饥饿值 ≤ 12 且有危险 | 10分钟 | 提醒注意补给 |
| 近处敌怪 | 新敌怪出现且 < 10格 | 3分钟 | 警告危险 |
| 食物分享 | 你有食物且玩家血量 < 70% | 5分钟 | 主动分享食物 |
| 矿洞暗处 | 地下 + 亮度 < 7 | 10分钟 | 建议插火把 |
| 玩家远离 | 距离 > 50格 | 1分钟 | 关心你去哪 |
| 玩家归来 | 距离 < 30格 | 1分钟 | 开心迎接 |

### 上下文注入（静默更新，不触发回复）

| 检测项 | 说明 |
|--------|------|
| 手持物品变化 | 玩家换了手里的东西时自动感知 |
| 发现新结构 | 128格内发现村庄、地牢等结构时记录 |
| 女仆背包变化 | 背包物品列表变化时静默更新 |
| 容器交互 | 打开/关闭容器时记录（用于活动推断） |
| 钓鱼开始 | 使用鱼竿时记录（用于判断进入钓鱼节奏） |

这些信息会静默注入 LLM 上下文，女仆可能以此为契机主动搭话。

### 陪玩式感知（自动触发，无需操作）

除基础感知外，插件还会自动进行以下陪玩行为：

| 能力 | 说明 |
|------|------|
| 安静陪伴 | 玩家稳定处于适合陪伴的状态（挖矿、建家、钓鱼、红石工程、下界/末地探索等）一段时间后，低频说一句陪玩话 |
| 主动建议 | 暗处缺火把时提示补光、建造时有材料主动说能帮忙、钓鱼时陪着等 |
| 小游戏陪伴 | 下棋时开局/中盘/结局自然陪玩式回应 |
| 活动状态追踪 | 自动推断玩家处于挖矿、建家、钓鱼、赶路、红石工程、下界/末地探索、村民交易等状态，以此调整陪玩方式 |
| 游戏内目标板 | 在 Minecraft 右上角 HUD 显示当前 Minecraft 目标和步骤，可由 AI 结构化更新、插件面板编辑或玩家输入 |

> 安静陪伴在战斗中、危险探索、整理物品、刷怪收尾等不适合打扰的场合不会触发，不用担心被打断。

## 八、事件推送

以下游戏事件会自动推送给 N.E.K.O，女仆会做出相应反应：

- 女仆受伤/死亡
- 玩家受伤（聚合统计，不会刷屏）、死亡
- 玩家击杀实体（聚合统计）
- 解锁成就
- 切换群系
- 天气变化（下雨/打雷/放晴）
- 昼夜交替
- 背包物品变化（别人给了你东西/拿走了东西）
- 方块活动（连续破坏/放置方块会聚合推送）
- 容器交互
- 钓鱼开始/钓到物品
- 棋局开始/中盘/结束（五子棋/国际象棋/中国象棋）
- 维度切换（进入/离开下界、末地）
- 玩家聊天

## 九、游戏内指令（一般用不到）

| 指令 | 说明 |
|------|------|
| `/neko accept <id>` | 确认执行 AI 请求的指令 |
| `/neko reject <id>` | 拒绝 AI 请求的指令 |
| `/neko plan <text>` | 设置游戏内右上角 HUD 显示的目标板文本 |
| `/neko plan clear` | 清除 HUD 目标板显示 |

## 十、常见问题

**Q: 插件显示未连接？**
A: 确保 Minecraft 已启动且进入了游戏世界，WebSocket 端口未被占用，且游戏未暂停且进入存档。

**Q: 不知道是 Minecraft 桥接问题还是 N.E.K.O 宿主问题？**
A: 先在插件面板点击“诊断桥接”。它会检查 Java/Minecraft 进程、WebSocket、mod 配置、女仆状态和已指定女仆。模型、TTS、全局工具调用策略属于 N.E.K.O 宿主侧，需要在宿主侧排查。

**Q: 女仆不听话？**
A: 检查是否已正确指定女仆（在仪表盘或通过对话指定），同时人设不要和mc冲突。

**Q: 想让女仆帮忙插火把但她说没有？**
A: 女仆背包里需要有火把。给她几根火把就好了。

**Q: 女仆频繁说话太吵？**
A: 优先把插件配置 `plugin.toml` 中的 `companion_mode` 改成 `quiet`。如果使用 `custom`，再手动调大 `playmate_quiet_stable_seconds`、`playmate_quiet_cooldown`、`playmate_suggestion_cooldown`。

**Q: 想让 AI 执行指令但总是被拒绝？**
A: 需要在模组配置中将 `commandExecutionEnabled` 设为 `true`，然后当游戏弹出确认框时点击接受。

**Q: 下棋时女仆说话太多/太少？**
A: 可以在 `plugin.toml` 中调整 `playmate_minigame_feedback_cooldown`（默认 90 秒，调大则更少说话）和 `playmate_minigame_context_chars`（默认 90 字符，控制每次回复最大长度）。

**Q: 安静陪伴触发太频繁/太少？**
A: 调整 `playmate_quiet_stable_seconds`（多久没动静才触发，默认 90 秒）和 `playmate_quiet_cooldown`（陪伴发言冷却，默认 300 秒）。
